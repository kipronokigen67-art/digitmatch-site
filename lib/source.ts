import {z} from 'zod';
const urlSchema=z.string().url().refine(value=>{const u=new URL(value);if(!['http:','https:'].includes(u.protocol))return false;const host=u.hostname.toLowerCase();const blocked=/^(localhost|.*\.local|127\.|10\.|192\.168\.|169\.254\.|172\.(1[6-9]|2\d|3[01])\.)/.test(host);return !blocked&&!host.endsWith('metadata.google.internal')&&host!=='169.254.169.254'},'URL targets a prohibited private or internal network');
export function analyzeSource(input:string){if(input.endsWith('.zip'))return {type:'ARCHIVE',message:'Archive uploads require the signed upload endpoint and isolated extraction worker.'};if(input.includes('github.com/')||input.includes('gitlab.com/')||input.includes('bitbucket.org/')){urlSchema.parse(input);return {type:'GIT',message:'Repository source accepted. Connect a Git provider before importing private code.'};}urlSchema.parse(input);return {type:'WEBSITE',message:'Public URL accepted for authorized analysis. Redirects are revalidated server-side.'};}
export {urlSchema};
