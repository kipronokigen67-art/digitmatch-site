export default function Docs(){return <main className="mx-auto max-w-4xl px-6 py-16"><p className="text-sm text-cyan">PrimeHost AI documentation</p><h1 className="mt-3 text-5xl font-bold">Infrastructure that tells the truth.</h1><div className="prose prose-invert mt-10 max-w-none"><h2>Deployment lifecycle</h2><p>Requests are authenticated, authorized against the organization, quota-checked, idempotently recorded, and queued. Workers transition deployments through QUEUED, PREPARING, BUILDING, VALIDATING, UPLOADING, DEPLOYING, HEALTH_CHECK, and LIVE. A failed health check cannot replace the previous production pointer.</p><h2>Provider configuration</h2><p>Database, queue, object storage, hosting, DNS, certificates, authentication, Git, AI, and billing are server-side integrations. Configure them through secret management; the app reports missing configuration rather than presenting mock success.</p><h2>Build security</h2><p>Untrusted source code must run in a disposable sandbox, never in the API process. Apply resource limits and deny access to internal networks. Archive extraction must enforce size, file-count, path, symlink, and malware policies.</p></div></main>}
